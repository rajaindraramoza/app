<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.rajaindra">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Raja Indra"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RajaIndra"
        tools:ignore="AllowBackup">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RumahSakit" />
        <activity android:name=".RSArifinAchmad" />
        <activity android:name=".Masjid" />
        <activity android:name=".Bank" />
        <activity android:name=".BankBRI" />
        <activity android:name=".Mall" />
        <activity android:name=".MallSKA" />
    </application>

</manifest>